# Katafract screenshot pipeline — fastlane snapshot config

project("./ExifArmor.xcodeproj")

# cmfmbp (ARM64) has both iOS 26.4 and 26.4.1 runtimes; devices are under 26.4.1.
# Pin to 26.4.1 so fastlane passes OS=26.4.1 to xcodebuild (not the 26.4 fallback).
ios_version("26.4.1")

devices([
  "iPhone 17 Pro Max",
  "iPad Pro 13-inch (M5)",
])

languages(["en-US"])

scheme("ExifArmorScreenshots")

output_directory("./fastlane/screenshots")
clear_previous_screenshots(true)
override_status_bar(true)
override_status_bar_arguments("--time 9:41 --batteryState charged --batteryLevel 100")

launch_arguments([
  "--screenshots",
  "--mock-prices",
])

skip_open_summary(true)
concurrent_simulators(true)